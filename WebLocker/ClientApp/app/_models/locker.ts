export class Locker {
    constructor(public hasDevice: boolean, public name: string, public id: number, public DeviceId: number, public LockerId: number, public CabinetId: number) { }
}                                                                                  
                                                                                  